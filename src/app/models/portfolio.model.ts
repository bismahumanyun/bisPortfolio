export interface PortfolioItem {
    title: string;
    description: string;
    image: string;
}

export interface ExperienceItem {
    company: string;
    role: string;
    duration: string;
    description: string;
}

export interface Skill {
    name: string;
    level: number;
}